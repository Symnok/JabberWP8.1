<phone:PhoneApplicationPage
    x:Class="JabberWP.Pages.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"
    Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="send picture" Click="SendPicture_Click"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,17,12,8">
            <TextBlock x:Name="name_tblck" FontSize="32" TextTrimming="WordEllipsis"/>
            <TextBlock x:Name="presence_tblck" FontSize="18" Opacity="0.6"
                       TextTrimming="WordEllipsis"/>
        </StackPanel>

        <ListBox x:Name="messages_lstb" Grid.Row="1" Margin="7,0,7,0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="4" Padding="10,7,10,7" MaxWidth="330"
                            Background="{Binding IsOutgoing, Converter={StaticResource OutgoingToBrush}}"
                            HorizontalAlignment="{Binding IsOutgoing, Converter={StaticResource OutgoingToAlignment}}"
                            Tap="Bubble_Tap">
                        <StackPanel>
                            <Image Source="{Binding ImageUri}" MaxHeight="260" Stretch="Uniform"
                                   HorizontalAlignment="Left"
                                   Visibility="{Binding IsImage, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="{Binding Body}" FontSize="21" TextWrapping="Wrap"
                                       Visibility="{Binding IsText, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="tap to open" FontSize="14" Opacity="0.7" Margin="0,3,0,0"
                                       Visibility="{Binding HasUrl, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="{Binding TimeText}" FontSize="14" Opacity="0.6"
                                       HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock x:Name="status_tblck" Grid.Row="2" Margin="11,0,11,4"
                   FontSize="16" Opacity="0.7" TextWrapping="Wrap" Visibility="Collapsed"/>

        <Grid Grid.Row="3" Margin="7,4,7,7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="message_tbx" Grid.Column="0" FontSize="22"
                     TextWrapping="Wrap" AcceptsReturn="False" KeyUp="Message_KeyUp"/>

            <Button x:Name="send_btn" Grid.Column="1" Content="send" Margin="4,0,0,0"
                    VerticalAlignment="Bottom" Click="Send_Click"/>
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>
